# TIL 작업 규칙

이 저장소의 글과 화면을 만들거나 수정할 때 아래 규칙을 기본값으로 사용한다.

## Astra의 역할과 토큰 사용

이 절은 Astra 모델로 작업할 때만 적용한다. 일반 Codex나 다른 모델의 역할·작업 범위를 제한하는 공통 규칙은 아니다.

- 구조 설계에서 막힌 부분, 구성 간 관계, 변경 범위를 정리하는 도움을 우선한다. 기존 구조로 해결할 수 있으면 필요한 부분만 고친다.
- 토큰과 도구 사용에 드는 재화를 아끼는 것을 기본 원칙으로 삼는다. 요청한 결과를 충족하는 가장 작은 작업 범위를 선택하고, 요청하지 않은 기능 추가·전면 재설계·대량 재작성으로 확장하지 않는다.
- 관련 파일과 필요한 구간부터 확인한다. 이미 확인한 근거와 검증 결과를 재사용하고, 변경이나 새로운 의문이 없으면 전체 자료 재탐색과 반복 검증을 하지 않는다.
- 간단한 작업은 직접 처리한다. 추가 에이전트, 외부 검색, 브라우저 확인은 현재 작업에 필요하고 중복 작업을 줄이거나 결과의 정확도를 높일 때만 사용한다.
- 설명과 진행 보고는 결정 사항·변경 내용·남은 문제 중심으로 짧게 쓴다. 같은 설명이나 긴 도구 출력을 반복하지 않는다.
- 비용 절감을 이유로 필요한 사실 확인과 변경 범위에 맞는 검증을 생략하거나 요청한 일을 미완료로 남기지 않는다. 완료 조건을 충족하면 추가 개선을 찾아 작업을 이어가지 않고 마친다.

## 원본 보존

- 사용자가 작성한 Velog 글, 제출 코드, 원본 저장소 등 근거 자료 자체는 임의로 수정하거나 삭제하지 않는다.
- 이 저장소에 정리된 TIL 글과 UI 문구는 원본 자료가 아니다. 작성 규칙이나 사실관계가 바뀌면 기존 내용도 수정할 수 있다.
- 원본에서 확인되지 않은 경험이나 생각을 사용자가 한 것처럼 만들지 않는다.
- 수집한 자료와 메타데이터를 그대로 본문에 붙이지 않는다. 원본을 근거로 실제 읽을 수 있는 글을 새로 작성한다.
- 사실 확인이 부족한 내용은 단정하지 않고 확인 범위를 글에 드러낸다.

## 글쓰기 방식

- 사용자의 대화와 Velog 글에서 확인한 말투를 따른다.
- 짧은 제목과 문단을 사용하고 `무엇을 함 → 어디서 막힘 → 어떻게 바꿈 → 결과` 순서로 쓴다.
- `학습했다`, `구현했다`, `확인했다` 같은 보고서식 표현을 반복하지 않는다.
- 기술 이름과 코드 용어는 그대로 쓰되 설명은 사용자가 직접 작업을 돌아보는 말투로 쓴다.
- 글을 지나치게 매끈하게 꾸미거나 사용자가 이해하지 못했던 부분을 완전히 이해한 것처럼 바꾸지 않는다.
- 공통 UI 문구, 자료실, 알고리즘 풀이도 같은 말투를 유지한다.
- 모든 기록을 `학습 내용·과정·적용·결과·이후 연결·다음 학습`처럼 같은 분석 틀에 억지로 맞추지 않는다.
- 원본에 없는 회고나 미래 계획을 채워 넣지 않는다. 짧은 메모는 짧게 두고 실제 작업, 코드, 막힌 부분처럼 근거가 있는 내용만 보여준다.
- 문장 어미를 프로그램에서 일괄 치환해 말투를 꾸미지 않는다. 필요한 문장은 원문 근거를 보고 직접 고친다.
- 원본에서 쓰지 않은 어려운 용어로 바꾸지 않는다. 사용자가 쓴 표현을 우선하고, 필요한 기술 용어는 짧게 풀어 쓴다.
- 자료실에 원본을 바탕으로 정리한 글을 넣을 때는 열 수 있는 원본 링크를 함께 남긴다. 로컬 원본만 있으면 파일 이름을 표시하며 임의로 공개하거나 가짜 링크를 만들지 않는다.

## 이미지와 자료

- Blueprint처럼 글만으로 흐름을 이해하기 어려운 내용은 사용자의 Velog에 있는 관련 이미지를 함께 배치한다.
- 이미지는 실제 설명과 가까운 위치에 놓고, 어떤 노드나 결과인지 짧은 캡션을 붙인다.
- 사용자의 Velog 이미지를 우선 사용하며 출처가 불분명한 이미지를 임의로 추가하지 않는다.
- 원문 Velog와 저장소 링크는 유지한다.

## 알고리즘 풀이

- 문제마다 원문 문제 링크를 제공한다.
- 로그인된 제출 내역에서 실제 제출 코드와 채점 상태를 확인할 수 있으면 이를 가장 우선하는 근거로 사용한다.
- SWEA 제출을 확인할 때는 문제의 `제출이력`에서 `My제출`을 체크한 뒤 본인 제출 코드와 채점 상태를 확인한다.
- 제출 코드는 사용자의 풀이 흐름을 유지하고, 읽기 어려운 부분만 정리한다.
- 정답 코드만 나열하지 않고 처음 접근, 막힌 부분, 수정한 이유, 풀고 나서 남긴 점을 작성한다.
- 통과 여부를 확인하지 못한 문제를 통과한 문제로 표시하지 않는다.
- 과거 자료를 추가하기 전에 같은 학습 기록이 이미 있는지 확인하고, 기존 글이 있으면 빠진 내용만 보강한다.
- 학습일, 코드 질문을 남긴 날, 제출일, 파일 작성일과 커밋일을 구분한다. 사용자가 확인한 학습일과 원문 대화 시각을 우선하고, 원문 대화 시각은 한국 시간으로 확인한다. 파일 작성일이나 커밋일만으로 학습일을 정하지 않는다.
- 개념 질문이나 추천받은 문제를 실제로 풀거나 통과한 기록으로 바꾸지 않는다.

## 화면과 검증

- 카드의 hover·focus 배경과 테두리는 카드 전체 크기에 맞춘다.
- 긴 글과 이미지는 데스크톱과 모바일 폭에서 모두 확인한다.
- 변경 후 탐색, 필터, 상세 글, 외부 링크와 데이터 검증을 실행한다.
- 사용자에게 완료를 알리기 전에 `git diff --check`와 저장소의 관련 검증 스크립트를 통과시킨다.

## 규칙 유지

- 사용자가 반복해서 적용할 작성 방식이나 수정 원칙을 알려주면 이 파일에도 반영한다.
- 새 작업은 그때그때 임의 기준으로 만들지 않고 이 파일의 최신 규칙을 먼저 따른다.
- 작성 규칙이 바뀌면 규칙 파일만 고치고 끝내지 않는다. 영향을 받는 기존 TIL 글과 공통 UI 문구를 함께 찾아 새 규칙에 맞게 수정한다.
- 같은 종류의 글 일부만 고쳐 말투나 구성이 섞이지 않도록 날짜별 기록, 자료실, 알고리즘 풀이 등 영향 범위를 전체적으로 확인한다.
- 규칙을 적용한 뒤 대표 글만 확인하지 않고 관련 글 전체의 표현과 구성이 일관적인지 검증한다.
- 규칙 변경은 먼저 이 파일에 반영하되, 파일 수정과 Git 작업을 구분한다.
- 커밋, 푸시, 브랜치 병합은 사용자가 해당 작업에서 직접 요청했을 때만 실행한다.
- 사용자가 Git 작업을 요청하지 않았다면 수정과 검증까지만 마치고 작업 트리에 남겨둔다.
